sources:
  ex1:
    description: this source doesn't work
    driver: csv
    args: {}
  ex2:
    description: this source doesn't work
    metadata:
      foo: 'bar'
      bar: [1, 2, 3]
    driver: csv
    args: {}
