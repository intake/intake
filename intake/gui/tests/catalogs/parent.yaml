sources:
  child1:
    args:
      path: "{{CATALOG_DIR}}/catalog1.yaml"
    driver: intake.catalog.local.YAMLFileCatalog

  child2:
    args:
      path: "{{CATALOG_DIR}}/catalog2.yaml"
    driver: intake.catalog.local.YAMLFileCatalog

  entry1:
    driver: blah
    args:
      urlpath: ''
