version: 2

build:
  os: ubuntu-22.04
  tools:
    python: miniconda3-4.7
  commands:
    - cd docs && python plugins.py

conda:
  environment: docs/environment.yml

python:
  install:
    - method: pip
      path: .

sphinx:
  configuration: docs/source/conf.py
